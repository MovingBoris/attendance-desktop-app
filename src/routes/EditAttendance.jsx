// This will be worked on in class, the current code i have does not work and breaks.
// Just filler code for now

function EditAttendance() {
    return(
        <div>Hello</div>
    );
};

export default EditAttendance;